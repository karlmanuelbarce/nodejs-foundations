info:
  name: put
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:3000/notes/1
  body:
    type: json
    data: |-
      {
        "title": "Hello",
        "content": "World"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
