info:
  name: post notes
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/notes/
  body:
    type: json
    data: |-
      {
        "title": "Test",
        "content": "World"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
