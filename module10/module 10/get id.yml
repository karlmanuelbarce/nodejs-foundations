info:
  name: get id
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/notes/1
  body:
    type: json
    data: |-
      {
        "title": "Hello",
        "content": "test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
