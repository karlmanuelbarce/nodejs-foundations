info:
  name: server-post
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3000/users
  body:
    type: json
    data: |-
      {
        "name": "Karl",
        "age": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
